<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.goldenhand.bleakfalls.gpacalculator.ResultSummary$PlaceholderFragment"
    android:descendantFocusability="blocksDescendants"
    android:id="@+id/included"
    android:background="@color/black" >

    <TableLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:background="@color/gray" >

        <TextView
            android:id="@+id/overallGpaItem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NO GPA YET"
            android:textColor="@color/white" />
    </TableLayout>

    <Button
        android:id="@+id/addSubjectButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_string"
        android:textColor="@color/white"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        style="?android:attr/buttonBarStyle" />

    <Button
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:id="@+id/fakeButton"/>

    <ListView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/addSubjectButton"
        android:cacheColorHint="#00000000"
        android:dividerHeight="0dp"
        android:divider="@null" />

</RelativeLayout>
